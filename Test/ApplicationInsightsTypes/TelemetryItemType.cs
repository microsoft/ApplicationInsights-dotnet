namespace AI
{
    public enum TelemetryItemType
    {
        Event,
        Exception,
        Message,
        Metric,
        PageView,
        PageViewPerformance,
        PerformanceCounter,
        RemoteDependency,
        Request,
        SessionState,
        Availability
    }
}
